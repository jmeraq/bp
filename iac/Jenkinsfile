pipeline {
    agent {
        docker { image 'jmeraq/tools:latest' }
    }
    stages {
        stage('Test') {
            steps {

                sh "cd iac/"
                sh "echo $PWD"
                sh "ls -la"
                sh 'terraform init'
                sh 'terraform plan'
            }
        }
    }
}