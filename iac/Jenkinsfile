pipeline {
    agent {
        docker { image 'jmeraq/tools:latest' }
    }
    stages {
        stage('Test') {
            steps {
                sh "cd iac/ && terraform init && terraform plan"
            }
        }
    }
}