pipeline {
    agent {
        docker { image 'jmeraq/tools:latest' }
    }
    stages {
        stage('Test') {
            steps {
                sh "ls -la"
                sh "cd $WORKSPACE/iac"
                sh 'terraform init'
                sh 'terraform plan'
            }
        }
    }
}