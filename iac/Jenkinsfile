pipeline {
    agent {
        docker { image 'jmeraq/tools:latest' }
    }
    stages {
        stage('Test') {
            steps {

                sh "/bin/bash -c 'cd iac/'"
                sh "echo $PWD"
                sleep 600
                sh "ls -la"
                sh 'terraform init'
                sh 'terraform plan'
            }
        }
    }
}