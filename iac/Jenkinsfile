pipeline {
    agent {
        docker { image 'jmeraq/tools:latest' }
    }
    stages {
        stage('Test') {
            steps {

                sh "cd $WORKSPACE/iac/"
                sh "ls -la"
                sh 'terraform init'
                sh 'terraform plan'
            }
        }
    }
}