pipeline {
    agent {
        docker { image 'jmeraq/tools:latest' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'terraform --version'
                sh 'aws --version'
            }
        }
    }
}